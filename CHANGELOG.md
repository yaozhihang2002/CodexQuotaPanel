# Changelog

## Unreleased

- 新增当前重置周期的每日 Token 汇总：悬停可查看精确输入、缓存输入、输出与推理用量，点击可展开每日及周期汇总。
- 本地日志按模型与 `Default` / `Fast` 分类；缺失 `service_tier` 的会话会使用首个明确速率回填，否则按 `Default` 处理，显式 `priority` 仍归入 `Fast`。依据 2026-08-24 的 OpenAI 公开 API 价格估算美元成本；Auto-review 等无公开费率的记录标记为“未公开计价”，不会被当作免费或猜测定价。
- 当服务端仅返回一个额度窗口时自动使用单环和单额度卡；第二个窗口恢复后自动恢复双环与双卡，并在显示器、远程桌面和窗口句柄变化后重新确认置顶状态。

## v0.4.1 — Pre-release

- 统一优化设置与详情界面的自绘按钮、选项、滑块和开关：圆角会先合成正确的父级背景，高 DPI 下不再从透明边缘露出相邻浅色控件，并补充更细致的描边、按压、悬停、禁用与键盘焦点反馈。
- 24 小时额度趋势支持鼠标悬停：自动吸附最近的数据点，显示本地时间和当时的剩余额度，并用引导线与高亮圆点标示对应位置。
- 悬浮球尺寸与设置字体比例不再响应鼠标滚轮；滚轮经过滑块或数字框时继续滚动设置页，避免误触修改。
- 修复取消、Esc 或关闭设置窗口后，悬浮球尺寸预览动画可能停留在未保存目标值的问题；关闭时始终恢复最后保存的设置。
- 趋势记录按窗口分别保存完整 24 小时逐分钟观测值，取消小变化跳过规则，不通过降采样换取容量，并保留原有本地数据格式兼容性。
- 按页面构建、设置状态、详情交互和应用协调职责继续拆分大型界面文件，保持原有配置格式与操作方式不变。
- 新增本地一键发布脚本：应用只发布一次，安装器、便携包、校验文件和其它附件复用同一份已验证产物。

下载文件：

- 普通用户：`CodexQuotaPanel-0.4.1-Setup.exe`
- 便携使用：`CodexQuotaPanel-0.4.1-portable-x64.zip`
- 中文 MSI：`CodexQuotaPanel-0.4.1-x64.msi`
- 完整源码：`CodexQuotaPanel-0.4.1-source.zip`
- SHA-256：`SHA256SUMS-v0.4.1.txt`

## v0.4.0 — Pre-release

- 新增本地智能续航预测：空闲区间会计入平均，并融合 90 分钟短期速度、6 小时长期速度与样本置信度；低置信度时不显示结论，避免一次突发消耗造成过度激进的倒计时，且不会读取或保存对话内容。
- 设置窗口在首帧显示后、用户空闲时分批预热隐藏页面；首次切换已预热选项卡时不再同步创建整页控件。
- 将整窗合成缩小到设置内容视口，并保持拉伸期间只更新当前页面，减少串行绘制、闪烁与残影。
- 拆分设置辅助控件、悬浮球绘制、详情控件与转场代码，保持原配置格式、操作入口和界面行为不变。
- 新增续航预测的深浅主题、中英文视觉矩阵，并继续覆盖最大字体、窗口拉伸和远程 DPI 回归。

下载文件：

- 普通用户：`CodexQuotaPanel-0.4.0-Setup.exe`
- 便携使用：`CodexQuotaPanel-0.4.0-portable-x64.zip`
- 中文 MSI：`CodexQuotaPanel-0.4.0-x64.msi`
- 完整源码：`CodexQuotaPanel-0.4.0-source.zip`
- SHA-256：`SHA256SUMS-v0.4.0.txt`

## v0.3.2 — Pre-release

- 修复远程桌面高 DPI 环境下悬浮球文字被重复缩放、相互遮挡的问题；圆环、文字与火焰现在统一按物理像素单次缩放。
- 监听远程会话连接、断开、登录和解锁事件，在显示环境变化后即时并延迟二次重算悬浮球位置、尺寸、圆角区域与动画预览缓存。
- 重新整理设置窗口的布局密度与字体缩放规则：窗口更紧凑，选项卡切换、滚轮重绘和打开过程更直接，并修复大字号下标题、说明文字与下拉项重叠或裁切的问题。
- 完善深色与浅色主题的卡片、圆角和控件配色；悬浮球默认保持深色背景，同时支持独立自定义背景颜色。
- 优化字体资源复用和延迟释放，减少设置预览、切换选项卡与高 DPI 重算时的闪烁和中间帧。
- 修复拖动设置窗口边框时多个已缓存页面重复布局、底色短暂外露的问题；拉伸期间只更新当前页面，并合成原生子控件后一次显示，降低闪烁与残影。
- 修复其他电脑在 150% / 200% 等高 DPI 下悬浮球即时预览仍按固定物理像素创建、导致预览过小或只显示局部的问题；预览尺寸与容器现在按目标显示器 DPI 同步缩放。
- 保持 v0.3.1 的设置文件兼容，覆盖安装后继续保留悬浮球位置、尺寸、透明度、主题和交互偏好。

下载文件：

- 普通用户：`CodexQuotaPanel-0.3.2-Setup.exe`
- 便携使用：`CodexQuotaPanel-0.3.2-portable-x64.zip`
- 仅需中文 MSI：`CodexQuotaPanel-0.3.2-x64.msi`

## v0.3.1 — Pre-release

- 移除自动安全模式：即使上次异常退出或电脑重启，下次启动也会按已保存的状态、位置和个性化设置显示。
- 托盘右键菜单新增“重启应用 / Restart app”，用于快速重新加载仍可响应的程序实例。
- 只有在安装确认页点击“安装”后，安装程序才会关闭正在运行的面板；如果安装前处于开启状态，成功安装或安装回滚后会自动重新启动。
- 修复覆盖安装时先弹出“文件正在使用中”的时序问题，最终点击“安装”后会先关闭面板，再由 Windows Installer 检查文件占用。
- 修复托盘右键菜单首次打开时“重启应用”显示为空白项的问题，并为中英文界面增加启动时文字兜底。
- 开启鼠标穿透时可勾选“不再提醒”；提醒开关可在“交互”设置中恢复，并支持设置导入与导出。
- 额度警告提供“本额度周期不再提醒”，同一重置周期只提醒一次，下个周期自动恢复。
- 保留脱敏崩溃诊断，但崩溃状态不再改变启动参数或用户设置。

下载文件：

- 普通用户：`CodexQuotaPanel-0.3.1-Setup.exe`
- 便携使用：`CodexQuotaPanel-0.3.1-portable-x64.zip`
- 仅需中文 MSI：`CodexQuotaPanel-0.3.1-x64.msi`

## v0.3.0 — Pre-release

- 新增崩溃恢复与安全模式：非正常退出后可用保守参数启动，并保留原始设置供恢复。
- 新增设置导入与导出；仅迁移可移植的外观、交互和提醒参数，不包含位置、历史、路径或账户数据。
- 新增内置 GitHub Release 更新检查，支持可选启动检查、手动检查、24 小时缓存与失败节流，不会自动下载或运行安装包。
- 完善多显示器和 Per-Monitor DPI 保护，兼容负坐标屏幕、跨屏缩放和可见区域恢复。
- 修复浅色主题切换后设置内容区残留黑色背景的问题，并整合“版本与更新”设置卡片。
- 三种火焰均升级为霜晶、冷焰、温焰、热焰、烈焰五档连续反馈；优化临界状态、温焰亮度、焰芯、粒子与像素融化过渡。
- 继续优化拖动、滚轮和界面切换时的重绘，减少透明窗口残影与闪烁。
- 设置改为带备份的原子写入，升级时继续保留旧版悬浮球位置及个性化设置。
- 新增自动化构建与手动确认的预发布候选工作流。

### 下载建议

- 普通用户：`CodexQuotaPanel-0.3.0-Setup.exe`
- 便携使用：`CodexQuotaPanel-0.3.0-portable-x64.zip`
- 仅需中文 MSI：`CodexQuotaPanel-0.3.0-x64.msi`

## v0.2.0 — Pre-release

- 详情面板改为统一的逻辑坐标与 DPI 布局，高缩放屏幕下窗口、控件和卡片内部绘制保持同一比例。
- 修复 125%—175% 缩放时面板底部出现大块空白、文字重叠和进度条错位的问题。
- 悬浮球拖动更新提高到约 120 FPS，在高刷屏上保持顺滑，同时合并高采样率鼠标的冗余事件。
- 拖动期间暂停火焰动画计时器，松开后恢复，降低透明悬浮窗的合成与重绘负担。
- 保留 v0.1.1 的位置持久化、首次显示恢复和中英双语安装改进。

### 下载建议

- 普通用户：`CodexQuotaPanel-0.2.0-Setup.exe`
- 便携使用：`CodexQuotaPanel-0.2.0-portable-x64.zip`
- 仅需中文 MSI：`CodexQuotaPanel-0.2.0-x64.msi`

## v0.1.1 — Pre-release

- 点击“保存并应用”后立即持久化设置，但不再关闭设置窗口。
- 保存后可以继续修改；取消或关闭只撤销上一次保存之后的未保存内容。
- 保存状态改为“更改已保存 · 可继续修改”，反馈更加明确。
- README 增加醒目的安装包与发布页面直达链接。
- 安装向导的“快捷方式”“安装目录”和“确认安装”页面现在可以正常双向返回。
- 欢迎页移除强硬的版权警告，改为简洁的 MIT 开源许可说明。
- 修复安装目录页可能出现的 Windows Installer 2707 错误。
- 再次运行同版本 Setup 时自动执行就地覆盖，无需先手动卸载。
- 退出程序时始终保存可恢复的悬浮球位置，修复重新打开后位置重置的问题。
- 设置同时写入本地 JSON 文件并兼容原注册表，避免注册表账户或权限差异导致位置保存失效。
- 修复窗体首次显示事件再次执行默认定位、导致悬浮球短暂出现在保存位置后跳回右下角的问题。

### 下载建议

- 普通用户：`CodexQuotaPanel-0.1.1-Setup.exe`
- 便携使用：`CodexQuotaPanel-0.1.1-portable-x64.zip`
- 仅需中文 MSI：`CodexQuotaPanel-0.1.1-x64.msi`

## v0.1.0 — Pre-release

CodexQuotaPanel 的首个公开测试版本。

### 主要内容

- 提供五小时与一周额度双环、动态托盘图标和三种火焰反馈样式。
- 支持深色、浅色、跟随系统主题，以及简体中文与英文界面。
- 支持置顶、透明度、鼠标穿透、自由拖动、悬浮球尺寸和字体大小调节。
- 改进悬浮球与详情面板的展开/收起过渡，减少闪烁、残影和方形中间帧。
- 提供额度提醒、免打扰时段、本地趋势与重置卡到期信息。
- 安装前可选择语言，默认简体中文；可选择安装目录和是否创建桌面快捷方式。
- 修复桌面快捷方式不显示应用 Logo 的问题。
- “数据与关于”页面加入 GitHub 项目入口和版本更新说明。

### 下载建议

- 普通用户：`CodexQuotaPanel-0.1.0-Setup.exe`
- 便携使用：`CodexQuotaPanel-0.1.0-portable-x64.zip`
- 仅需中文 MSI：`CodexQuotaPanel-0.1.0-x64.msi`

这是预发布版本，仍可能存在兼容性或界面问题。欢迎提交 Issue、Pull Request 和二创版本。
